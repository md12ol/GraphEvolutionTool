# Next session — 2026-08-10

Read `.claude/work/current/plan.md` and `.claude/work/decisions.md` first, then `.claude/work/hotfixes.md`.

**Where things stand:** Issue #51 is done — `common::best_index` merged to `main` at `9274f38` via
PR #55, verified (213 tests, clippy clean). This task is closing via `/done best-index`; by the
time this file is read it should already be archived to `work/archive/2026-08_best-index/`.

**Start here:** if this file is still being read, `/done best-index` didn't finish — resume it:
push `.claude/work/collab.md` (item #43) to `main`, then archive `work/current/`. Otherwise, the
next piece of work is GitHub **#53** (replace `target_profile_path` with an inline `target_profile`
array) — the other `(2)`-prefixed issue, per `gh issue list`.

**Watch out for:** `#56` (the follow-up sweep of both evolvers) is deliberately staged behind the
currently open issue set — don't pick it up early. `collab.md` #40 and #41 still await James's
acknowledgement; not blocking, just unresolved.

**⏰ Time-sensitive:** none.
